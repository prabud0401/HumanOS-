"""
Nervous System Organ — Real-time signals and live client connections for HumanOS.

Broadcasts events to WebSocket and SSE subscribers so operators see the twin's
pulse as it happens.
"""

default_app_config = "organs.nervous_system.apps.NervousSystemConfig"
